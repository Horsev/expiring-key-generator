{
  "name": "expiring-key-generator",
  "version": "1.3.0",
  "description": "Generate date-based expiring keys with SHA256 hashing and time-window validation",
  "type": "module",
  "main": "src/index.js",
  "exports": "./src/index.js",
  "files": ["src"],
  "keywords": ["key-generator", "expiring-key", "sha256", "base-n", "codec"],
  "engines": { "node": ">=16" },
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
